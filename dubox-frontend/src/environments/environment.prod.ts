export const environment = {
  production: true,
  apiUrl: 'https://api.dubox.me/api', // Update with your production backend URL
  apiVersion: 'v1',
  publicApiUrl: 'https://api.dubox.me/api/public', // Public API URL (no auth required)
};

